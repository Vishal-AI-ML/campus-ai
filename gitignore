# ---------------------------------------------------------------------------
# Campus AI - monorepo .gitignore (backend + ai-worker + frontend)
# Place at repo root: E:\campus-ai\.gitignore
# ---------------------------------------------------------------------------

# --- Secrets & environment (NEVER commit) ----------------------------------
.env
.env.*
!.env.example
*.pem
*.key

# --- Python (backend/ and ai-worker/) --------------------------------------
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.cache/

# --- uv ---------------------------------------------------------------------
# Keep uv.lock committed; ignore the local virtualenv only.
# (.venv already ignored above.)

# --- AI worker: local vector store / model caches --------------------------
.chroma/
chroma_db/
*.sqlite3

# --- Node / Next.js (frontend/) --------------------------------------------
node_modules/
.next/
out/
build/
.turbo/
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- Editors / OS ----------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# --- Logs -------------------------------------------------------------------
*.log
logs/

# --- Keep these (do NOT ignore) --------------------------------------------
# Alembic migrations  -> backend/alembic/versions/*.py  (commit them!)
# .env.example files  -> commit as templates
