# ---- Repo-root local junk (not part of the app) ----

# Packaging / delivery artifacts
*.zip
_zipstage/
_pkg/
_hk/

# Local extractions of the AI worker source (worker lives in its own HF Space).
# If you ever want the worker source tracked here, put it in a real folder
# (e.g. worker/) and remove the matching line below.
aiw-src/
ai-worker-src/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
